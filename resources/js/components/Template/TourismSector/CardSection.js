// CardSection.js
export default class CardSection {
    constructor(name, cards) {
      this.name = name; // e.g., "Infrastructure", "Natural Resources"
      this.cards = cards; // Array of cards with dynamic inputs
    }
  }
  